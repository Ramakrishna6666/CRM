# Spring Boot 3.2.0 / Java 21 - Upgraded Configuration
spring.jpa.hibernate.ddl-auto=create-drop
spring.datasource.url=jdbc:mysql://localhost:3306/crm?useSSL=false
spring.datasource.username=root
spring.datasource.password=password

management.endpoints.web.base-path=/appinfo

spring.thymeleaf.mode=HTML
spring.thymeleaf.cache=false

# Spring Boot 3.x: defer datasource initialization until after JPA setup
spring.jpa.defer-datasource-initialization=true
